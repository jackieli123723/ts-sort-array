{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --stream",
    "test:coverage": "lerna run test:coverage --stream",
    "bootstrap": "lerna bootstrap",
    "bootstrap:cnpm": "lerna bootstrap --registry=https://registry.npm.taobao.org",
    "clean": "lerna clean --yes",
    "build": "lerna run build --stream",
    "publish": "lerna publish --force-publish=*",
    "pravite-publish": "npm publish --access public",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff",
    "publish-c": "lerna publish --npm-tag=alpha --concurrency 1",
    "publish-e": "lerna publish --exact --concurrency 1 --force-publish",
    "publish-p": "lerna publish  from-package --no-verify-access --registry http://0.0.0.0:4873/"
  },
  "author": "jackieli",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jackieli123723/ts-sort-array"
  },
  "bugs": {
    "url": "https://github.com/jackieli123723/ts-sort-array/issues"
  },
  "homepage": "https://github.com/jackieli123723/ts-sort-array#readme",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
